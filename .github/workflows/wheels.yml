name: Wheels

on:
  workflow_dispatch:
  # Temporary: workflow_dispatch is only invocable from the default branch, so
  # this push trigger (scoped to this branch) lets us verify on an actual
  # GitHub Actions runner before merging to master. Remove once green.
  push:
    branches: [ci/phase2-wheels-macos]

jobs:
  build:
    name: build (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest, macos-latest]
    steps:
      - uses: actions/checkout@v5

      - name: Install Qt
        if: runner.os == 'Windows'
        uses: jurplel/install-qt-action@v4
        with:
          version: "6.11.1"
          modules: qt5compat
          # aqtinstall 3.3.* can't locate Qt 6.11+ on Windows - the online
          # repo dropped the combined qt6_XXXX/qt6_XXXX/Updates.xml in favor
          # of per-arch dirs (miurahr/aqtinstall#959). Use the patched
          # aqtinstall until a release containing PR #1000 lands on PyPI.
          aqtsource: git+https://github.com/miurahr/aqtinstall.git@8c3695d4a4e1ceabf6a74dc6c79681656dc6b74b

      - name: Set Qt env vars
        if: runner.os == 'Windows'
        shell: bash
        run: echo "CMAKE_PREFIX_PATH=$QT_ROOT_DIR" >> "$GITHUB_ENV"

      - name: Build wheels
        uses: pypa/cibuildwheel@v4.1.0

      - uses: actions/upload-artifact@v7
        with:
          name: wheels-${{ matrix.os }}
          path: wheelhouse/*.whl
